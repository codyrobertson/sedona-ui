// Demo script to test new components
console.log("Testing Badge, Input, and Card components");

// Badge variants to test
const badgeVariants = [
  'default', 'brand', 'secondary', 'destructive', 
  'safe', 'close', 'risk', 'outline', 'neutral', 'accent'
];

const badgeSizes = ['sm', 'default', 'lg'];

console.log("Badge variants:", badgeVariants);
console.log("Badge sizes:", badgeSizes);

// Input types to test
const inputTypes = [
  'text', 'email', 'password', 'number'
];

console.log("Input types:", inputTypes);

// Card components ready
console.log("Card components: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter");

console.log("All components use theme variables - no hardcoded CSS!");